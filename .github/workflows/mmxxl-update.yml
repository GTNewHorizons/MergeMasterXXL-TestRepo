
name: Update via MergeMasterXXL

on:
  # schedule:
  #   - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  update-and-tag:
    name: Update And Tag
    uses: GTNewHorizons/MergeMasterXXL/.github/workflows/update-dev.yml@master
    secrets: inherit
